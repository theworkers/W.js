    extend( W, {
        bind : bind,
        clone : clone,
        countedCallbackMixin : countedCallbackMixin,
        each : each,
        eventMixin : eventMixin,
        extend : extend,
        List : List,
        loop : loop,
        Middleware : Middleware,
        Sequence : Sequence,
        sequence : sequence,
        TickTimer : TickTimer,
        Timer : Timer,
        Object : Obj,
        isOk : isOk,
        isNotOk : isNotOk,
        Router : Router,
        interpose : interpose,
        partial : partial,
        flip : flip,
        partition : partition,
        flatten : flatten,
        Promise : Promise
    });
} ( W ) );
